# Here's a function that takes two `int`s and returns their sum as an `int`.
def plus(a, b):
    # Python automatically returns the value of the last expression.
    return a + b

# Call a function just as you'd expect, with `name(args)`.
res = plus(1, 2)
print("1+2 =", res)

